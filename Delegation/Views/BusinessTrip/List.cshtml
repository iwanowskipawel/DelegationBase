@model IEnumerable<BusinessTrip>


<div>liczba delegacji: @Model.Count()</div>
@foreach(var trip in Model)
{
    <div>
        <h3>Karta kilometrowa: @trip.KilometersCard</h3>
        Cel wyjazdu: @trip.Destination
        <h4>Nr tematu @trip.Project</h4>
    </div>
}